package robotlegs.bender.extensions.signalCommandMap.support
{
	/**
	 * @author creynder
	 */
	import org.osflash.signals.Signal;
	
	public class NullSignal extends Signal{
		public function NullSignal(...valueClasses)
		{
			super(valueClasses);
		}
	}
}